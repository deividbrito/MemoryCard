package memorycard.config;

public class Config {
    // IP do servidor (pode ser localhost ou IP da LAN)
    public static final String SERVIDOR_IP = "127.0.0.1";

    // porta padrão do servidor
    public static final int PORTA = 12345;

    // nom do jogador (pode ser definido dinamicamente futuramente)
    public static String nomeJogador = "Jogador";
}
